import React, { useState } from 'react';

const Form = props => {
    return (
        <form>
            <input />
            <button />
            <textarea />
        </form>
    );
};

export default Form;